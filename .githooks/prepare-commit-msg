#!/bin/sh
# Удаляем Co-authored-by: cursoragent из сообщения коммита.
# Использование: git config core.hooksPath .githooks
FILE=$1
if [ -z "$FILE" ] || [ ! -f "$FILE" ]; then exit 0; fi
# Всегда удаляем Co-authored-by: Cursor/cursoragent (чтобы срабатывало и при коммитах из IDE)
sed -i.bak '/^Co-authored-by:.*[Cc]ursor/d' "$FILE" 2>/dev/null || true
rm -f "${FILE}.bak" 2>/dev/null || true
