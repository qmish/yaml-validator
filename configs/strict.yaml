# Строгие правила стиля (в духе yamllint)
rules:
  check_syntax: true
  check_duplicates: true
  check_integrity: true
  check_common_errors: true
  style:
    require_document_start: true
    forbid_trailing_spaces: true
    require_newline_at_eof: true
    forbid_consecutive_empty_lines: true
    require_document_end: false
    require_comments_indented: false
    require_quoted_keys: false
  k8s_schema:
    enabled: false
  required_fields: []
  max_line_length: 200
  sensitive_patterns:
    - password
    - secret
    - token
    - key
