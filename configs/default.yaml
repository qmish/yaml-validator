rules:
  check_syntax: true
  check_duplicates: true
  check_integrity: true
  check_common_errors: true
  required_fields:
    - apiVersion
    - kind
    - metadata.name
  max_line_length: 200
  sensitive_patterns:
    - password
    - secret
    - token
    - key
