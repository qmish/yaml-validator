# Пример конфигурации с проверкой по произвольной JSON Schema
# Использование: yaml-validator validate file.yaml -c configs/json-schema.yaml
# schema_path — путь к файлу (относительный или абсолютный) или URL (https://...)
# schema_cache_dir — при URL-схеме каталог для кэша (пусто = без кэша)
rules:
  check_syntax: true
  check_duplicates: true
  check_integrity: false
  check_common_errors: true
  style:
    require_document_start: false
    forbid_trailing_spaces: false
    require_newline_at_eof: false
  json_schema:
    enabled: true
    schema_path: testdata/schemas/config-schema.json
    # schema_cache_dir: ""  # при schema_path по URL — каталог для кэша
  k8s_schema:
    enabled: false
  required_fields: []
  max_line_length: 200
