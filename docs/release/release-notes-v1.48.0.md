# Изменения в v1.48.0

## v1.48.0

- **Схема по URL** (6.6) — `rules.json_schema.schema_path` поддерживает URL (`https://...`). Опция `schema_cache_dir` кэширует загруженные схемы локально для повторных валидаций без сетевых запросов.
