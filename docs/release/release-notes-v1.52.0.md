# Изменения в v1.52.0

## v1.52.0

- **Мультидокумент YAML** (6.7) — валидация каждого документа в файле отдельно. Ошибки содержат индекс документа (`DocumentIndex`), вывод — `file#docN` (например `config.yaml#doc2:5: message`). JsonSchema и K8sSchema применяются к первому документу.
